# Data Integrity Checklist: Model and Provider Assignment

**Created**: 2026-06-08 | **Feature**: [spec.md](../spec.md)

> Requirements-quality checklist ("Unit Tests for English") for the Data Integrity domain of E005. Each item questions whether the spec / plan / data-model adequately define a data-integrity concern. Audience: Reviewer (PR).

## Completeness

- [X] CHK001 Are the requirements explicit about which store holds the fleet default (config.json `defaultModel`) versus the per-agent assignment (localStorage `LS_AGENTS` / `PersistedAgent`), so a reviewer can confirm the two are not conflated? [Completeness, FR-007 / data-model substrate table / AD-002] <!-- Evaluator: Covered by plan.md §Storage + data-model.md §Substrate Table + AD-002 (config.json defaultModel vs LS_AGENTS/PersistedAgent kept distinct) -->
- [X] CHK002 Do the requirements state that BOTH the fleet default and every per-agent assignment must survive an application restart, not just one of them? [Completeness, FR-007 / SC-002 / SC-005 / DR-9] <!-- Evaluator: Covered by FR-007 (persist per-agent AND fleet default) + SC-002 (per-agent restart) + SC-005 (fleet default restart) + DR-9 -->
- [X] CHK003 Is the exact set of persisted per-agent fields enumerated (canonical `model` reused, NEW `assignmentSource`) and is it stated which are existing versus newly added? [Completeness, data-model Entity Table / AgentAssignment `[1]`] <!-- Evaluator: Covered by data-model.md §Entity Table (reused `model` EXISTING, `assignmentSource` NEW, `providerId` DERIVED-not-stored) + note [1] -->
- [X] CHK004 Do the requirements define what an "absent" assignment means on the persisted record (no `model` + no `assignmentSource` ⇒ role-based fallback) versus a stored value, so the empty case is unambiguous on round-trip? [Completeness, DR-8 / data-model `[1]` / state-machine note] <!-- Evaluator: Covered by data-model.md note [1] (ABSENT = no `model` + no `assignmentSource` ⇒ role-based) + state-machine note + DR-8 -->
- [X] CHK005 Is the creation-time precedence (explicit assignment → fleet default → role-based fallback) fully specified as the rule that determines which model is persisted? [Completeness, FR-008 / DR-8 / US1.4] <!-- Evaluator: Covered by DR-8 (explicit → FleetDefault → role-based) + FR-008 + US1 acceptance scenario 4 -->
- [X] CHK006 Do the requirements specify that the GOD agent writes the same persisted fields (`model` + `assignmentSource`) through the same mechanism, so no second/divergent persistence path exists? [Completeness, FR-013 / DR-10] <!-- Evaluator: Covered by DR-10 (GOD writes same `model` + `assignmentSource`, no separate path) + FR-013 -->
- [X] CHK007 Are the four capability flags that feed the gap computation explicitly named (`supportsImages`, `supportsMcpTools`, `supportsWebSearch`, `supportsCaching`) so the read-only source of truth is complete? [Completeness, FR-009 / data-model CapabilityDescriptor / DR-3] <!-- Evaluator: Covered by data-model.md CapabilityDescriptor entity (all four flags named) + DR-3 + FR-009 -->

## Consistency

- [X] CHK008 Is the provider consistently described as DERIVED (never stored) across spec, plan, and data-model, with no requirement implying a stored second provider field? [Consistency, FR-008 / AD-001 / HINT-001 / DR-1] <!-- Evaluator: Covered by FR-008 + AD-001 + HINT-001 + DR-1 (provider derived, never stored) — consistent across all three artifacts -->
- [X] CHK009 Do the requirements consistently use a single canonical identifier (`modelId` / the reused `Agent.model`) as the stored field, rather than alternating between "model" and "provider+model" as stored data? [Consistency, AD-001 / DR-1 / data-model AgentAssignment] <!-- Evaluator: Covered by AD-001 (store `modelId`, derive provider) + DR-1 + data-model AgentAssignment (reused `Agent.model` is the only stored id) -->
- [X] CHK010 Is the `assignmentSource` enumeration consistent everywhere (`explicit` | `fleet-default`, with `role-based` explicitly NOT a persisted value)? [Consistency, data-model AgentAssignment / state-machine note / DR-8] <!-- Evaluator: Covered by data-model AgentAssignment (`explicit | fleet-default`) + state-machine note ("not a stored 'role-based' value") + DR-8 -->
- [X] CHK011 Are the persistence substrates named consistently between plan (config.json / `LS_AGENTS` / `PersistedAgent`) and data-model (`config.json` / `localStorage['cth.agents']`), with no contradiction about the localStorage key? [Consistency, AD-002 / data-model substrate table] <!-- Evaluator: Covered — plan `LS_AGENTS` and data-model `cth.agents` are the constant-name vs literal value of the SAME key (verified: store.ts `const LS_AGENTS = 'cth.agents'`); no contradiction -->
- [X] CHK012 Is the fleet default consistently mapped to the EXISTING `HarnessConfig.defaultModel` field (reuse, not a new key) across plan and data-model? [Consistency, AD-002 / FR-005 / data-model FleetDefault] <!-- Evaluator: Covered by AD-002 (reuse config.json/`defaultModel`) + plan Storage + data-model FleetDefault (reuses EXISTING `HarnessConfig.defaultModel`) -->
- [X] CHK013 Do the stale-handling requirements consistently say "preserve + flag, never remap" with no requirement elsewhere allowing a fallback that overwrites the stored `modelId`? [Consistency, FR-011 / AD-006 / DR-5 / HINT-005] <!-- Evaluator: Covered by FR-011 + AD-006 + DR-5 + HINT-005 (preserve+flag, never remap) — no contradicting overwrite rule; DR-11 (added) also preserves stale default -->

## Clarity

- [X] CHK014 Is "derived on read" defined precisely enough (`providerId = lookupModelInfo(modelId)?.provider.id`) that a reviewer can confirm no derivation result is persisted back? [Clarity, DR-1 / AD-001] <!-- Evaluator: Covered by DR-1 (exact formula `providerId = lookupModelInfo(modelId)?.provider.id`, "computed on read") + AD-001 -->
- [X] CHK015 Is the meaning of provenance ("explicit" vs "fleet-default"/inherited) defined unambiguously, including how the UI distinguishes "using fleet default (M)" from "custom: M2"? [Clarity, FR-004 / US3.3 / SC-006 / glossary] <!-- Evaluator: Covered by FR-004 + US3 scenario 3 ("using fleet default (M)" vs "custom: M2") + SC-006 + glossary "Explicit vs inherited" -->
- [X] CHK016 Is "stale/unavailable" defined as a DERIVED state (`lookupModel(model) === null` on an agent that HAS a `model`) rather than a stored flag, so reviewers know staleness is recomputed and not persisted? [Clarity, DR-5 / data-model state machine] <!-- Evaluator: Covered by DR-5 ("staleness is DERIVED — `lookupModel(model) === null`") + state-machine stale row ("staleness is DERIVED") -->
- [X] CHK017 Is "validation = registry resolution" stated clearly enough to distinguish an invalid input (rejected at set) from an unresolvable-but-preserved stale id? [Clarity, DR-2 / FR-011 / error-handling table] <!-- Evaluator: Covered by DR-2 (validation = registry resolution; unresolvable id = STALE not invalid input) + plan Error Handling table (Validation row vs Stale row) + FR-011 -->
- [X] CHK018 Is "snapshot-at-creation" defined clearly enough that a reviewer can tell exactly what is frozen onto the record (effective `model` + `assignmentSource`) at agent-creation time? [Clarity, AD-005 / HINT-002 / DR-4] <!-- Evaluator: Covered by AD-005 ("Snapshot effective model + source onto the agent at creation") + HINT-002 + DR-4 (frozen at creation) -->

## Edge-Case Coverage

- [X] CHK019 Do the requirements define the round-trip behavior when an agent has an explicit assignment, is restarted, and the model still resolves (assignment unchanged)? [Edge-Case Coverage, SC-002 / DR-9 / US1.3] <!-- Evaluator: Covered by SC-002 (100% survive restart) + US1 scenario 3 (same provider+model after restart) + DR-9 round-trips -->
- [X] CHK020 Do the requirements define behavior when a persisted `modelId` no longer resolves after restart (preserved verbatim, flagged stale, prompt re-selection, never remapped)? [Edge-Case Coverage, FR-011 / DR-5 / SC-008] <!-- Evaluator: Covered by FR-011 + DR-5 (preserve verbatim, flag stale, prompt re-selection, never remap) + SC-008 -->
- [X] CHK021 Is the empty/unreadable-registry case specified to fall back to existing role-based default behavior for creation while preserving any already-stored assignment? [Edge-Case Coverage, FR-012 / DR-7 / SC-008] <!-- Evaluator: Covered by FR-012 + DR-7 (empty registry → empty-state + role-based fallback) for creation; DR-5 unconditionally preserves any already-stored id (every id is unresolvable when registry is empty ⇒ stale, preserved) -->
- [X] CHK022 Do the requirements address the non-retroactive case: changing the fleet default after agents exist leaves every pre-existing agent's persisted `model`/`assignmentSource` unchanged? [Edge-Case Coverage, FR-006 / DR-4 / SC-004 / US2.3] <!-- Evaluator: Covered by FR-006 + DR-4 (MUST NOT mutate existing `Agent.model`/`assignmentSource`) + SC-004 + US2 scenario 3 -->
- [X] CHK023 Is the "revert to fleet default" transition specified to re-resolve the CURRENT default onto the record and re-set `assignmentSource = 'fleet-default'`, and does it persist across restart? [Edge-Case Coverage, FR-004 / state machine / SC-006 / US3.2] <!-- Evaluator: Covered by state-machine revert row ("re-resolve current default onto `model`, `assignmentSource = 'fleet-default'`") + FR-004 + SC-006 (persists across restart) + US3 scenario 2 -->
- [X] CHK024 Is the re-selection transition from a stale assignment to a resolvable model defined (overwrite `model`, `assignmentSource = 'explicit'`)? [Edge-Case Coverage, DR-5 / state machine] <!-- Evaluator: Covered by state-machine "stale → explicit" row (overwrite `model`, `assignmentSource = 'explicit'`) + DR-5 -->
- [X] CHK025 Do the requirements cover a stale FleetDefault (config `defaultModel` no longer resolves) and how creation behaves in that case (fall through to role-based fallback)? [Edge-Case Coverage, DR-7 / DR-8 / FleetDefault state] <!-- Evaluator: Resolved — added DR-11 + a `stale` FleetDefault state to data-model.md (present-but-unresolvable `defaultModel` falls through to role-based fallback at creation, preserved+flagged, never remapped) -->
- [X] CHK026 Is the missing-credential case specified as annotation-only (presence read-only) that never blocks selection and never writes credential data onto the assignment record? [Edge-Case Coverage, FR-010 / DR-6] <!-- Evaluator: Covered by FR-010 + DR-6 (presence read-only annotation, selection allowed, key material never read/stored/logged) -->

## Traceability

- [X] CHK027 Does every persistence requirement (FR-007) trace to a concrete substrate and round-trip in the Requirement Coverage Map and data-model, with no orphaned "persist" claim? [Traceability, FR-007 / Requirement Coverage Map / DR-9] <!-- Evaluator: Covered by FR-007 → plan Requirement Coverage Map (Store/config, localStorage + config.json round-trips) + DR-9 + data-model substrate table -->
- [X] CHK028 Is the derivation rule (DR-1) traceable to the FR it satisfies (FR-008) and the assumption that a model id uniquely determines its provider? [Traceability, DR-1 / FR-008 / Assumptions] <!-- Evaluator: Covered — DR-1 cites "FR-008; Assumptions" and spec Assumptions states "A model id uniquely determines its provider via the registry" -->
- [X] CHK029 Does the non-retroactivity rule trace consistently across FR-006, AD-005, DR-4, SC-004, and US2.3 with no gap in the chain? [Traceability, FR-006 / AD-005 / DR-4 / SC-004] <!-- Evaluator: Covered — chain intact: FR-006 → AD-005 (snapshot-at-creation) → DR-4 (non-retroactive) → SC-004 → US2 scenario 3 -->
- [X] CHK030 Does the stale-handling rule trace across FR-011, AD-006, DR-5, SC-008, and the named Risk? [Traceability, FR-011 / AD-006 / DR-5 / SC-008] <!-- Evaluator: Covered — chain intact: FR-011 → AD-006 → DR-5 (cites "FR-011; SC-008; Risk") → SC-008 → spec Risk "Stale assignment after a registry change" -->
- [X] CHK031 Is the additive-coexistence constraint with E004 traceable from the Risk row through HINT-004 and DR-9 to a measurable success criterion or coverage entry? [Traceability, HINT-004 / DR-9 / Risk table / SC-002 / SC-005] <!-- Evaluator: Covered — Risk row (shared drawer/config) → HINT-004 (do NOT widen E004 `providerKeys`) → DR-9 (cites "FR-007; SC-002, SC-005; Risk") → SC-002/SC-005 restart-survival -->

## Measurability

- [X] CHK032 Is the restart-survival requirement stated measurably (e.g., "100% of assignments survive restart") so a reviewer can confirm a pass/fail outcome? [Measurability, SC-002 / SC-005] <!-- Evaluator: Covered by SC-002 ("100% of assignments survive restart") + SC-005 (fleet default persists across restart) -->
- [X] CHK033 Is non-retroactivity stated measurably ("every pre-existing agent's assignment unchanged; only later-created agents inherit the new default") rather than as a soft goal? [Measurability, SC-004 / DR-4] <!-- Evaluator: Covered by SC-004 ("leaves every pre-existing agent's assignment unchanged and applies only to agents created after the change") + DR-4 -->
- [X] CHK034 Is the capability-gap output measurable (each of the four `false` flags is named; a fully-capable model yields zero warnings) so correctness of the derived warning is checkable? [Measurability, SC-007 / DR-3 / FR-009] <!-- Evaluator: Covered by SC-007 (names each unsupported capability; fully-capable → no warning) + DR-3 (name each of the four flags that is `false`) + FR-009 -->
- [X] CHK035 Is the stale outcome measurable (assignment flagged stale and prompts re-selection rather than switching providers) per SC-008? [Measurability, SC-008 / DR-5] <!-- Evaluator: Covered by SC-008 ("flagged stale and prompts re-selection rather than switching providers") + DR-5 -->

## Ambiguity

- [X] CHK036 Is it unambiguous that `assignmentSource` is never persisted as `'role-based'` and that role-based fallback is the ABSENCE of stored assignment, not a third stored value? [Ambiguity, DR-8 / data-model state-machine note / AgentAssignment] <!-- Evaluator: Covered by AgentAssignment ("absent ⇒ role-based fallback, never persisted as 'role-based'") + state-machine note + DR-8 -->
- [X] CHK037 Is it unambiguous whether `providerId` ever appears in the persisted JSON (it must not — derived only), removing any reader assumption that it is stored on `PersistedAgent` or in `config.json`? [Ambiguity, DR-1 / AD-001 / data-model ER notes] <!-- Evaluator: Covered by DR-1 + AD-001 + Entity Table/ER ("`providerId` DERIVED, NOT STORED") — explicitly never in persisted JSON -->
- [X] CHK038 Is the additive constraint unambiguous about NOT widening or altering E004's `providerKeys`/presence shape, so a reviewer can confirm no schema overlap or overwrite? [Ambiguity, HINT-004 / DR-9 / Risk table] <!-- Evaluator: Covered by HINT-004 ("Do NOT widen or alter E004 `providerKeys`/presence") + DR-9 (independently-keyed) + plan Risk Mitigation row -->
- [X] CHK039 Is it unambiguous that the warning, missing-credential, and empty-registry conditions affect UI/derivation only and never mutate or block the persisted assignment record? [Ambiguity, FR-009 / FR-010 / FR-012 / DR-3 / DR-6 / DR-7] <!-- Evaluator: Covered by FR-009/DR-3 (non-blocking warn), FR-010/DR-6 (annotate, do not block, no write), FR-012/DR-7 (empty-state + fallback) — all UI/derivation-only -->
- [X] CHK040 Is it unambiguous which field changes versus stays fixed on each AgentAssignment state transition (e.g., stale preserves `model` while explicit/revert overwrites it), per the state machine? [Ambiguity, data-model state machine / DR-5] <!-- Evaluator: Covered by state-machine "Persisted effect" column (stale: `model` UNCHANGED; explicit/revert: overwrite `model` + set `assignmentSource`) + DR-5 -->
