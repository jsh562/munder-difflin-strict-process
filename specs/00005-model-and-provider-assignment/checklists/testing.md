# Testing Checklist: Model and Provider Assignment

**Created**: 2026-06-08 | **Feature**: [spec.md](../spec.md)

> Requirements-quality checklist ("Unit Tests for English") for the Testing domain of E005. Each item questions whether the spec / plan / data-model define requirements, success criteria, edge-case outcomes, test tiers, invariants, and coverage traceability well enough to write pass/fail tests. This checks requirement/plan quality, NOT test execution. Audience: Reviewer (PR).

## Measurability

- [X] CHK001 Is each of FR-001..FR-014 phrased as an observable, verifiable outcome (a behavior or stored state a test can assert against) rather than an aspirational statement? [Measurability, FR-001..FR-014] <!-- Evaluator: Covered by spec.md §Functional Requirements — every FR uses "MUST present/allow/persist/display/handle/indicate/expose" with an observable behavior or stored-state outcome -->
- [X] CHK002 Are SC-001..SC-008 each stated with an unambiguous pass/fail threshold (e.g., SC-002 "100% of assignments survive restart") so a test can return a binary verdict? [Measurability, SC-001..SC-008] <!-- Evaluator: Covered by spec.md §Success Criteria — SC-002 "100% survive restart"; SC-003/004/007/008 each binary (inherits/unchanged/warning-or-none/empty-state) -->
- [X] CHK003 Is the capability-gap warning success criterion measurable enough to assert (each of the four `false` flags is named; a fully-capable model yields zero warnings)? [Measurability, SC-007 / DR-3 / FR-009] <!-- Evaluator: Covered by spec.md SC-007 + data-model.md DR-3 — "name each of the four flags that is `false`"; "fully-capable model shows no warning" -->
- [X] CHK004 Is the model-precedence outcome (explicit assignment → fleet default → role-based fallback) stated specifically enough that a test can assert exactly one model wins at each precedence level? [Measurability, FR-008 / DR-8 / US1.4] <!-- Evaluator: Covered by data-model.md DR-8 + spec.md FR-008/US1.4 — ordered precedence "explicit AgentAssignment → FleetDefault → role-based fallback" -->
- [X] CHK005 Is the non-retroactivity outcome measurable ("every pre-existing agent's assignment unchanged; only later-created agents inherit the new default") so a before/after comparison gives a clear verdict? [Measurability, SC-004 / DR-4] <!-- Evaluator: Covered by spec.md SC-004 + data-model.md DR-4 — pre-existing assignments unchanged; only later-created agents inherit -->
- [X] CHK006 Is the stale outcome stated measurably (assignment flagged stale and prompts re-selection, model NOT switched to another provider) so the assertion has a definite expected value? [Measurability, SC-008 / DR-5 / FR-011] <!-- Evaluator: Covered by spec.md SC-008/FR-011 + data-model.md DR-5 + State Machine "stale" row — id preserved verbatim, flagged stale, prompts re-selection, never remapped -->
- [X] CHK007 Is the empty-registry outcome measurable (picker shows empty-state AND agent creation still succeeds via existing default) so both halves are independently assertable? [Measurability, SC-008 / DR-7 / FR-012] <!-- Evaluator: Covered by spec.md SC-008/FR-012 + data-model.md DR-7 — empty-state AND fallback to role-based default, two assertable halves -->

## Completeness

- [X] CHK008 Does the plan's Testing Strategy table define which tier (unit / integration / manual) verifies persistence round-trip across restart? [Completeness, plan Testing Strategy / SC-002 / SC-005 / DR-9] <!-- Evaluator: Resolved — added a "Persistence round-trip homing" note to plan §Testing Strategy: fleet-default config round-trip = Integration; per-agent cth.agents restart round-trip = app-smoke/manual; serialized assignment shape = Unit -->
- [X] CHK009 Does the plan identify WHAT the unit tier must cover for `src/shared/assignment.ts` (effective-model resolution, precedence, capability-gap computation, stale detection, provenance)? [Completeness, plan Testing Strategy / HINT-003] <!-- Evaluator: Covered by plan §Testing Strategy Unit row — names "effective-model resolution, precedence (explicit→default→role), capability-gap computation, stale detection, provenance" -->
- [X] CHK010 Does the plan specify WHERE each critical path is verified — vitest on electron-free `assignment.ts` vs config round-trip vs app-smoke/manual UI — with no behavior left without a home tier? [Completeness, plan Testing Strategy / HINT-003] <!-- Evaluator: Covered by plan §Testing Strategy (Unit/Integration rows + UI app-smoke note), reinforced by the new persistence-homing note — every critical path now has a named home tier -->
- [X] CHK011 Does the plan define which tier verifies the fleet-default config round-trip (set → persist → reload)? [Completeness, plan Testing Strategy (Integration row) / SC-005 / FR-007] <!-- Evaluator: Covered by plan §Testing Strategy Integration row — "Fleet-default config round-trip (set→persist→reload) via pure serialization" -->
- [X] CHK012 Does the plan state which behaviors are manual-only (picker render, drawer flow, Settings fleet-default control via app-smoke) so the boundary between automated and manual coverage is explicit? [Completeness, plan Testing Strategy note] <!-- Evaluator: Covered by plan §Testing Strategy note — "UI behaviors (picker render, drawer flow, Settings fleet-default control) are verified by app-smoke (manual)" -->
- [X] CHK013 Does the plan define a test approach for the non-retroactive inheritance snapshot (snapshot-at-creation, default change does not mutate existing agents)? [Completeness, AD-005 / DR-4 / FR-006 / plan Testing Strategy] <!-- Evaluator: Covered by plan AD-005 + HINT-002 + data-model DR-4 (snapshot-at-creation, non-retroactive) verified via Unit row "precedence/provenance" on assignment.ts -->
- [X] CHK014 Is the GOD-agent programmatic-assignment path's verification approach defined (same mechanism, same persistence + warning behavior, no second path)? [Completeness, FR-013 / DR-10] <!-- Evaluator: Covered by data-model DR-10 + spec FR-013 + Coverage Map FR-013 → assignment.ts/index.ts — "same mechanism... no separate programmatic path"; exercised by the same Unit-tier resolution logic -->
- [X] CHK015 Does the plan's Error Handling Strategy enumerate a defined response for each error category (validation, capability gap, missing credential, stale, empty registry) so each has an assertable expected behavior? [Completeness, plan Error Handling Strategy] <!-- Evaluator: Covered by plan §Error Handling Strategy table — all five categories enumerated, each with a defined Response -->

## Edge-Case Coverage

- [X] CHK016 Does the stale-model edge case have a defined, assertable expected outcome (preserve `modelId` verbatim, flag stale, prompt re-selection, never remap)? [Edge-Case Coverage, FR-011 / DR-5 / Edge Cases] <!-- Evaluator: Covered by spec.md Edge Cases/FR-011 + data-model DR-5 + State Machine "stale" row — preserve verbatim, flag, prompt re-selection, never remap -->
- [X] CHK017 Does the empty/unreadable-registry edge case have a defined expected outcome (empty-state in picker + fallback to existing role-based default)? [Edge-Case Coverage, FR-012 / DR-7 / Edge Cases] <!-- Evaluator: Covered by spec.md Edge Cases/FR-012 + data-model DR-7 — empty-state + fallback to existing role-based default -->
- [X] CHK018 Does the missing-credential edge case have a defined expected outcome (annotate "needs credentials", selection allowed, never blocked, no key material touched)? [Edge-Case Coverage, FR-010 / DR-6 / Edge Cases] <!-- Evaluator: Covered by spec.md Edge Cases/FR-010 + data-model DR-6 — "needs credentials" affordance, selection allowed, presence-only (no key material) -->
- [X] CHK019 Does the capability-gap edge case have a defined expected outcome (warning names each missing capability; assignment still succeeds)? [Edge-Case Coverage, FR-009 / DR-3 / US4] <!-- Evaluator: Covered by spec.md FR-009/US4 + data-model DR-3 — warning names each missing capability; assignment still succeeds -->
- [X] CHK020 Does the no-default fallback edge case have a defined expected outcome (no fleet default and no pick ⇒ existing role-based fallback, no explicit assignment recorded)? [Edge-Case Coverage, US1.4 / DR-8 / SC-003] <!-- Evaluator: Covered by spec.md US1 Acceptance Scenario 4 + SC-003 + data-model DR-8 — role-based fallback, no explicit assignment recorded -->
- [X] CHK021 Does the combined uncredentialed-AND-capability-gapped edge case have a defined expected outcome (both annotations surfaced together, neither blocks, assignment still succeeds)? [Edge-Case Coverage, Edge Cases / FR-009 / FR-010] <!-- Evaluator: Covered by spec.md Edge Cases bullet (line 52) — both annotations surfaced together, neither blocks, assignment succeeds (FR-009, FR-010) -->
- [X] CHK022 Does the stale-FleetDefault edge case (config `defaultModel` no longer resolves) have a defined expected outcome (fall through to role-based fallback at creation, preserved + flagged, never remapped)? [Edge-Case Coverage, DR-11 / FleetDefault state] <!-- Evaluator: Covered by data-model DR-11 + State—FleetDefault — present-but-unresolvable default falls through to role-based at creation, preserved+flagged, never remapped -->
- [X] CHK023 Does the concurrent-edit-with-E004 edge case have a defined expected outcome (assignment and credential fields additive and independently keyed, neither overwrites the other)? [Edge-Case Coverage, Edge Cases / HINT-004 / DR-9] <!-- Evaluator: Covered by spec.md Edge Cases (line 53) + plan HINT-004 + data-model DR-9 — additive, independently-keyed, no overwrite of E004 providerKeys -->

## Consistency

- [X] CHK024 Is the plan's Testing Strategy table consistent with the Requirement Coverage Map — does every P1 requirement mapped to `+src/shared/assignment.ts` fall within the named unit-tier scope? [Consistency, plan Testing Strategy / Requirement Coverage Map] <!-- Evaluator: Covered — Coverage Map FR-004/006/008/009/011/013 → assignment.ts all fall within Unit row scope (provenance, precedence, capability-gap, stale detection, resolution) -->
- [X] CHK025 Are the mock boundaries stated consistently (unit tier injects registry lookups and is electron-free; integration keeps Electron IPC behind thin type-only wrappers) with no behavior assigned to two conflicting tiers? [Consistency, plan Testing Strategy / HINT-003] <!-- Evaluator: Covered by plan §Testing Strategy — Unit "Inject registry lookups; electron-free (no IPC)" vs Integration "Electron IPC behind thin wrappers (type-only import)"; no double-assigned behavior -->
- [X] CHK026 Is the manual/app-smoke designation for UI behaviors consistent between the Testing Strategy note and the Requirement Coverage Map entries for UI components (picker, drawer, Settings)? [Consistency, plan Testing Strategy / Requirement Coverage Map] <!-- Evaluator: Covered — Testing Strategy note designates picker/drawer/Settings UI as app-smoke; Coverage Map lists those same components without contradicting (their logic split to assignment.ts unit tier) -->
- [X] CHK027 Do the Error Handling Strategy responses agree with the spec Edge Cases and FRs they implement (e.g., stale "preserve + flag" matches FR-011, capability gap "Save stays enabled" matches FR-009)? [Consistency, plan Error Handling Strategy / FR-009 / FR-011] <!-- Evaluator: Covered — Error Handling "preserve + flag / never remap" matches FR-011/DR-5; "warn... Save stays enabled" matches FR-009/SC-007; all five rows agree with their FRs/Edge Cases -->

## Clarity

- [X] CHK028 Is the unit-tier mock boundary defined precisely enough ("inject registry lookups; electron-free, no IPC") that a reviewer can tell what is real versus mocked in an `assignment.ts` test? [Clarity, plan Testing Strategy / HINT-003] <!-- Evaluator: Covered by plan §Testing Strategy Unit row + HINT-003 — "Inject registry lookups; electron-free (no IPC)"; registry is the only injected seam, all resolution logic is real -->
- [X] CHK029 Is "stale detection" defined clearly enough to test (a DERIVED state `lookupModel(model) === null` on an agent that HAS a `model`) rather than a stored flag a test would assert on? [Clarity, DR-5 / data-model state machine] <!-- Evaluator: Covered by data-model DR-5 + State Machine — staleness is DERIVED `lookupModel(model) === null` on an agent that HAS a model, explicitly "not a stored flag" -->
- [X] CHK030 Is "validation = registry resolution" stated clearly enough to test the distinction between a rejected invalid `set` and an unresolvable-but-preserved stale id? [Clarity, DR-2 / plan Error Handling Strategy / FR-011] <!-- Evaluator: Covered by data-model DR-2 ("valid iff lookupModel non-null; an unresolvable id is NOT invalid input — it is STALE") + plan Error Handling rows (Validation reject vs Stale preserve) -->
- [X] CHK031 Is "snapshot-at-creation" defined clearly enough that a test knows exactly what is frozen (effective `model` + `assignmentSource`) versus re-resolved live? [Clarity, AD-005 / HINT-002 / DR-4] <!-- Evaluator: Covered by plan AD-005 + HINT-002 + data-model DR-4 — effective `model` + `assignmentSource` frozen onto the record at creation; default change does not re-resolve live -->

## Edge-Case Coverage (Invariants)

- [X] CHK032 Is the warn-don't-block invariant expressed as a testable assertion (warning non-blocking, Save/confirm action stays enabled, assignment still recorded)? [Edge-Case Coverage, FR-009 / SC-007 / US4.1 / DR-3] <!-- Evaluator: Covered by spec FR-009/SC-007/US4.1 + data-model DR-3 — non-blocking warning, action stays enabled, assignment still recorded -->
- [X] CHK033 Is the never-remap-on-stale invariant expressed as a testable assertion (stored `modelId` unchanged; provider is NOT switched)? [Edge-Case Coverage, FR-011 / DR-5 / SC-008] <!-- Evaluator: Covered by spec FR-011/SC-008 + data-model DR-5 + State Machine — stored `model` UNCHANGED, provider never auto-remapped -->
- [X] CHK034 Is the additive-coexistence-with-E004 invariant expressed as a testable assertion (assignment fields do not widen or overwrite `providerKeys`/presence)? [Edge-Case Coverage, HINT-004 / DR-9 / Risk table] <!-- Evaluator: Covered by plan HINT-004 + data-model DR-9 + Risk Mitigation row — assignment fields additive/independently-keyed, do not widen or overwrite providerKeys/presence -->
- [X] CHK035 Is the provider-derived-not-stored invariant expressed as a testable assertion (`providerId` never appears in persisted JSON; always recomputed via `lookupModelInfo`)? [Edge-Case Coverage, DR-1 / AD-001 / HINT-001] <!-- Evaluator: Covered by data-model DR-1 + plan AD-001 + HINT-001 — `providerId` DERIVED NOT STORED; only `modelId` persisted; recomputed via `lookupModelInfo` on read -->

## Traceability

- [X] CHK036 Does every P1 user story (US1..US4) trace to a test approach in the plan via its Independent Test line and a Testing-Strategy tier, with no P1 left untested? [Traceability, US1..US4 Independent Test / plan Testing Strategy] <!-- Evaluator: Covered — US1-US4 each have an Independent Test line; their logic maps to the Unit tier (resolution/precedence/gap) + Integration (config round-trip) + app-smoke (UI flows); no P1 untested -->
- [X] CHK037 Does every Success Criterion (SC-001..SC-008) trace to a tier in the Testing Strategy or an explicit manual/app-smoke designation, with no SC left without a verification home? [Traceability, SC-001..SC-008 / plan Testing Strategy] <!-- Evaluator: Covered — SC-001/003/006 logic→Unit; SC-005→Integration; SC-002→app-smoke restart + Unit shape (per new persistence-homing note); SC-007 gap→Unit; SC-008 empty/stale→Unit; SC-001 picker→app-smoke -->
- [X] CHK038 Does each FR mapped to `+src/shared/assignment.ts` in the Requirement Coverage Map (FR-004, FR-006, FR-008, FR-009, FR-011, FR-013) trace to a named unit-tier behavior, confirming the Map and Testing Strategy are mutually consistent? [Traceability, Requirement Coverage Map / plan Testing Strategy] <!-- Evaluator: Covered — FR-004→provenance/revert, FR-006→snapshot/precedence, FR-008→precedence, FR-009→capability-gap, FR-011→stale detection, FR-013→same-mechanism resolution — all named Unit-row behaviors -->
- [X] CHK039 Are persistence/round-trip claims (FR-007, SC-002, SC-005) traceable to the concrete substrate and round-trip the integration/unit tier asserts, with no orphaned "persist" claim lacking a test home? [Traceability, FR-007 / SC-002 / SC-005 / DR-9 / plan Testing Strategy] <!-- Evaluator: Resolved — new persistence-homing note in plan §Testing Strategy ties SC-005/fleet-default→Integration (config.json), SC-002/per-agent→app-smoke (cth.agents restart) + Unit (serialized shape); no orphaned persist claim -->
- [X] CHK040 Are UI-only requirements (FR-001 picker render, FR-012 empty-state, FR-014 scope message) explicitly traced to the manual/app-smoke tier rather than silently assumed covered by unit tests? [Traceability, FR-001 / FR-012 / FR-014 / plan Testing Strategy note] <!-- Evaluator: Covered by plan §Testing Strategy note — picker render (FR-001), drawer/empty-state (FR-012), Settings fleet-default control + scope message (FR-014) designated app-smoke/manual; FR-012/FR-014 underlying fallback/scope logic also in Unit tier -->
