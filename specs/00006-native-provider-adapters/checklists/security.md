# Security Checklist: Native Provider Adapters

**Created**: 2026-06-09 | **Feature**: [spec.md](../spec.md)

## API Key Handling

- [X] CHK001 Do the requirements state that the API key is read ONLY from the injected environment (NATIVE_PROVIDER_API_KEY) and from no other source? [Completeness, FR-013] <!-- Evaluator: Covered by spec.md FR-013 ("read ... only from the injected environment (E004 seam)") + plan HINT-001 (worker entry is the only place that reads process.env) -->
- [X] CHK002 Is the single location permitted to read process.env for the key unambiguously named (worker entry only), so no adapter-core path reads it directly? [Clarity, plan HINT-001/AD-002] <!-- Evaluator: Covered by plan HINT-001 ("the worker entry is the only place that reads process.env"), AD-002 (selection in worker on process.env.NATIVE_PROVIDER_ID), and Constraints ("adapter core kept electron-free + fetch-injected") -->
- [X] CHK003 Do the requirements enumerate every sink the key MUST NEVER reach — AgentEvents, telemetry, transcripts, the hive, and logs — rather than naming only a subset? [Completeness, FR-013 / spec Compliance Check Secrets row] <!-- Evaluator: Resolved — amended FR-013 to enumerate the full sink set (AgentEvents, telemetry, transcripts, the hive, logs, api-error/diagnostic messages), matching the plan Security test tier surface -->
- [X] CHK004 Is "never write or emit the key" stated as an absolute prohibition (no redaction/truncation exception) so partial key exposure is also disallowed? [Clarity, FR-013] <!-- Evaluator: Resolved — amended FR-013 to state an absolute prohibition with no redaction/truncation exception and partial-key/substring exposure equally disallowed -->
- [X] CHK005 Do the requirements define the desk's behavior when no key is present at launch (clear "needs credentials" state, no broken loop) without that state echoing the absent/partial credential? [Completeness, spec Edge Cases / plan Error Handling "Missing key at launch"] <!-- Evaluator: Covered by spec Edge Cases ("surfaces a clear 'needs credentials' state rather than erroring obscurely") + plan Error Handling "Missing key at launch" (guard, do not start a broken loop); FR-013 absolute prohibition (now incl. error/diagnostic paths) ensures the state does not echo the credential -->
- [X] CHK006 Is it stated that auth headers carrying the key are constructed only at the HTTP/fetch boundary and not retained in any normalized turn or event payload? [Consistency, AD-003 / FR-007] <!-- Evaluator: Resolved — amended FR-013 to state auth headers are constructed only at the HTTP/fetch boundary and never retained in any normalized turn/event/usage payload; consistent with AD-003 (hand-rolled fetch) and FR-007 (no SDK/wire types past boundary) -->
- [X] CHK007 Do the requirements trace the key non-leak guarantee to a verifiable assertion (a test that the key string is absent from events/usage/logs) rather than leaving it as prose only? [Testability, plan Security test tier / FR-013] <!-- Evaluator: Covered by plan Testing Strategy Security tier (string-absence check across emitted events, usage, telemetry, transcripts, hive, logs) + Coverage Map FR-013 ("never emitted (test-asserted)") -->
- [X] CHK008 Is the relationship between FR-013 (env-only, never emitted) and ADR-0007 / project-instructions Governance stated consistently, with no conflicting allowance? [Consistency, FR-013 / spec Compliance Check Secrets row] <!-- Evaluator: Covered by spec Compliance Check "Secrets (ADR-0007): PASS — FR-013, env-only key, never emitted" + plan Instructions Check "Secrets never to events/telemetry/hive (ADR-0007): PASS"; no conflicting allowance present -->


## Provider SDK / Wire-Type Containment

- [X] CHK009 Do the requirements state that NO provider SDK or wire types cross the adapter boundary downstream? [Completeness, FR-007] <!-- Evaluator: Covered by spec FR-007 ("MUST NOT leak provider SDK/wire types past the adapter boundary") + plan Key Constraint + Constraints + AD-003 -->
- [X] CHK010 Is it defined that provider-specific data carrying secrets, auth headers, or raw request internals is normalized away before any AgentEvent is emitted? [Clarity, FR-007 / AD-003] <!-- Evaluator: Covered by FR-007 (emit normalized stream; no SDK/wire types past boundary) + amended FR-013 (auth headers never retained in any turn/event/usage payload) + research §1–2 (all divergence resolves INSIDE the adapter, normalized onto agentEvent) -->
- [X] CHK011 Does the plan justify the hand-rolled fetch + SSE choice as eliminating SDK types to contain (so there is nothing carrying provider internals to leak)? [Traceability, AD-003] <!-- Evaluator: Covered by plan AD-003 rationale ("nothing to contain for FR-007 (no SDK types)") -->
- [X] CHK012 Are the only artifacts allowed to cross the boundary explicitly bounded to the normalized turn/event/usage contract, excluding raw provider response objects? [Completeness, FR-007 / spec Key Entities ProviderRuntime port] <!-- Evaluator: Covered by FR-007 (normalized AgentEvent stream at the event-contract version) + Key Entities ProviderRuntime port ("downstream consumers depend on it, not on any provider") + research Summary ("All divergence ... normalize onto the existing agentEvent / token-usage / notification / api-error contract") -->
- [X] CHK013 Do the requirements address that raw SSE/error response bodies (which may echo request headers) are not forwarded verbatim into downstream events? [Completeness, FR-007 / FR-012] <!-- Evaluator: Covered by FR-007 (no wire types past boundary) + plan Error Handling (error classification surfaces status/category, not raw body) + amended FR-013 (no key/header substring on any error/diagnostic path); CHK028 confirms error-path scoping -->


## Reasoning / Thinking Content

- [X] CHK014 Do the requirements forbid replaying reasoning/thinking content back into a subsequent provider request where the provider forbids it? [Completeness, FR-003 / spec Edge Cases] <!-- Evaluator: Covered by FR-003 ("MUST NOT replay reasoning content back into a subsequent provider request") + spec Edge Cases ("Reasoning/thinking content is never replayed back into the provider request ... where the provider forbids it") -->
- [X] CHK015 Is reasoning content's allowed destination (surfaced as thinking, distinct from final answer) and its forbidden destination (next request context) both stated? [Clarity, FR-003 / SC-002] <!-- Evaluator: Covered by FR-003 (route reasoning→thinking, message→text; not replayed) + SC-002 ("shown as thinking, separate from the final answer, and is not replayed into a subsequent provider request") -->
- [X] CHK016 Do the requirements ensure reasoning/thinking is not used as a channel that re-exposes request internals (e.g., the key, headers, or raw request) downstream? [Completeness, FR-003 / FR-007] <!-- Evaluator: Resolved (transitively) — amended FR-013 absolute prohibition spans every sink/payload incl. event payloads; FR-007 forbids wire types past boundary, so the thinking channel (a normalized event) cannot carry the key/header/raw request; FR-003 routes reasoning to thinking only -->
- [X] CHK017 Is the no-replay constraint stated for BOTH providers (DeepSeek reasoning_content and Minimax thinking blocks) rather than only DeepSeek? [Consistency, FR-003 / FR-005 / research §1–2] <!-- Evaluator: Covered by spec Edge Cases (provider-agnostic "Reasoning/thinking content is never replayed ... where the provider forbids it") covering both; FR-005 surfaces Minimax thinking blocks; research §1 (DeepSeek 400-on-replay) + §2 (Minimax thinking) -->


## Capability-Degradation Notices

- [X] CHK018 Do the requirements state that a capability-degradation notice carries only the capability name and model, not the sensitive request content that triggered it? [Completeness, FR-010] <!-- Evaluator: Resolved — amended FR-010 to bound the notice payload to capability name + model id, excluding the triggering request content; consistent with research §4 example notice ("web search unsupported by <model>; skipped") -->
- [X] CHK019 Is the notice content bounded so it cannot echo dropped image bytes, raw tool inputs, or other sensitive request payload? [Clarity, FR-010 / research §4] <!-- Evaluator: Resolved — same FR-010 amendment explicitly forbids echoing dropped image bytes, raw tool inputs, or other request payload; research §4 (strip the unsupported field, proceed; image→text placeholder) -->
- [X] CHK020 Is "one notice per capability per session, not repeated every turn" defined so the channel cannot become a repeated leakage surface? [Consistency, FR-010 / AD-005 / HINT-004] <!-- Evaluator: Covered by FR-010 ("MUST NOT repeat for the same capability every turn within a session") + AD-005 (one notification per capability per session) + HINT-004 (emit exactly one notification per capability per session, dedupe) -->


## Single-Committer / Least-Privilege

- [X] CHK021 Do the requirements state that the worker NEVER writes shared runtime/hive/coordination state, only the single-committer main process does? [Completeness, AD-007 / Principle III] <!-- Evaluator: Covered by plan Constraints ("single-committer main stays the only writer of shared runtime state") + Instructions Check III ("registry fixes are source edits not runtime writes") + AD-007 (registry is static code, not runtime shared state) -->
- [X] CHK022 Is the registry capability-flag correction (FR-014) defined as a static source edit, explicitly NOT a runtime worker write? [Clarity, AD-007 / FR-014] <!-- Evaluator: Covered by AD-007 (chosen: static source edit of providerRegistry.ts; read-only at runtime) + Coverage Map FR-014 ("source edit if a flag misrepresents support") + plan Constraints -->
- [X] CHK023 Is the registry descriptor accessed read-only at runtime by the adapter, with write authority confined to source code? [Consistency, AD-007 / plan Constraints] <!-- Evaluator: Covered by AD-007 ("read-only at runtime"; correction by editing the static registry data module in source) + Integration Points FR-010/014 (adapter gates on lookupCapabilities; correct wrong flags in source) -->
- [X] CHK024 Does the spec's non-blocking Plan note (confirm FR-014 routes through single-committer main, not a worker write) have a corresponding resolved decision in the plan? [Traceability, spec Compliance Check note / AD-007] <!-- Evaluator: Covered by plan Policy Auditor verdict ("AD-007 static-source-edit satisfies the single-committer note") resolving spec Compliance Check non-blocking note via AD-007 -->
- [X] CHK025 Are the worker's privileges scoped to what the agentic loop needs (provider HTTP + injected env + existing tool seam), with no broadened write access introduced? [Completeness, Principle III / plan Integration Points] <!-- Evaluator: Covered by plan Integration Points (provider HTTP via fetch; reads NATIVE_PROVIDER_API_KEY/ID from env; executeTool exposes existing hive tools — "verify/extend so memory/mailbox/tasks work"; no new write authority) + spec Scope Excluded ("No new agent tools ... no new tools are introduced") -->


## Error / Diagnostic Paths

- [X] CHK026 Do the requirements state that api-error messages MUST NOT echo the API key or auth headers? [Completeness, FR-013 / FR-012] <!-- Evaluator: Resolved — amended FR-013 to name "api-error/diagnostic messages" in the sink set and to cover "any error/diagnostic path", so api-error messages may not echo the key or Authorization header (any substring) -->
- [X] CHK027 Are retry/backoff diagnostics (attempt counts, Retry-After, status codes) defined so they exclude credential and header content? [Clarity, AD-006 / FR-012] <!-- Evaluator: Covered by AD-006 + plan Error Handling (diagnostics are status/category, Retry-After, attempt counts) + amended FR-013 absolute prohibition on every error/diagnostic path; the diagnostic surface is bounded to non-credential fields -->
- [X] CHK028 Is the error-classification path (retryable vs terminal) defined to surface status/category without forwarding the raw authenticated request that produced it? [Consistency, FR-012 / plan Error Handling Strategy] <!-- Evaluator: Covered by FR-012 (classify retryable-vs-terminal) + plan Error Handling Strategy (responses are status/category + api-error, not the raw request) + amended FR-013 (no header retained in any payload) -->
- [X] CHK029 Do the requirements address that a 401/403 (auth failure) error is surfaced without echoing the rejected key or the Authorization header value? [Completeness, FR-012 / FR-013] <!-- Evaluator: Resolved — amended FR-013 (partial-key AND Authorization-header-value exposure disallowed on every error/diagnostic path) covers 401/403 surfacing; FR-012 classifies 401/403 as terminal, ending the turn cleanly -->
- [X] CHK030 Is the error tool result fed back to the model (for malformed-JSON self-correction) bounded so it does not embed request secrets? [Completeness, FR-011 / FR-013] <!-- Evaluator: Covered by FR-011 (error tool result for self-correction is the parse failure, not the request) + amended FR-013 absolute prohibition spanning the success path and any error/diagnostic path (the fed-back tool result is such a path) -->
- [X] CHK031 Are the secret-handling constraints stated consistently across the success path AND every error/diagnostic path (no path-specific exception)? [Consistency, FR-013 / plan Error Handling Strategy] <!-- Evaluator: Resolved — amended FR-013 states the prohibition holds "on either the success path or any error/diagnostic path ... no ... exception is permitted"; consistent with plan Error Handling Strategy -->


## Traceability & Verification

- [X] CHK032 Is each secret-related requirement (FR-013, FR-007, FR-003, FR-010) traceable to a Compliance Check row or an Instructions Check gate? [Traceability, spec Compliance Check / plan Instructions Check] <!-- Evaluator: Covered — FR-013→Compliance "Secrets (ADR-0007)" + Instructions Check "Secrets never to events/telemetry/hive"; FR-007→Compliance "Principle V" + Instructions Check V; FR-003/010→Compliance "Principle I" / "Degrade-gracefully" + Instructions Check; Coverage Map maps all four -->
- [X] CHK033 Does the Security test tier in the plan name a concrete, automatable check (string-absence of the key across events/usage/logs) rather than deferring entirely to manual review? [Testability, plan Testing Strategy Security tier] <!-- Evaluator: Covered by plan Testing Strategy Security tier ("assert by test that no adapter path writes the key into the full FR-013 surface set ... via a string-absence check"); not deferred to manual -->
- [X] CHK034 Is the absence of OS-keychain hardening recorded as an accepted MVP risk so the plaintext-config secret store is a known, bounded gap rather than an unstated assumption? [Completeness, spec Assumptions / project-instructions Governance] <!-- Evaluator: Resolved — added a spec Assumptions note recording the plaintext-config at-rest store + deferred safeStorage hardening as an accepted bounded MVP residual risk owned by E004/ADR-0007; E006 adds no new at-rest store -->
- [X] CHK035 Are the secret/non-leak requirements free of NEEDS CLARIFICATION markers and internal contradictions across spec and plan? [Consistency, spec/plan] <!-- Evaluator: Covered — no NEEDS CLARIFICATION markers in spec/plan; amended FR-013 sink set now matches the plan Security test tier surface and ADR-0007 with no conflicting allowance -->

