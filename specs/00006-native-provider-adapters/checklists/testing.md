# TESTING: Native Provider Adapters
**Created**: 2026-06-09 | **Feature**: [spec.md](../spec.md)

Requirements-quality checklist ("Unit Tests for English") for the Testing domain of E006. Each item interrogates whether the spec and plan adequately DEFINE testability — testable requirements, measurable success criteria, a defined fixture/mock/manual coverage model, and a traceable test strategy — not whether tests pass.

## Testability of Requirements & Success Criteria

- [X] CHK001 Is each functional requirement FR-001..FR-014 phrased so a single observable pass/fail behavior can be asserted, rather than an aspirational quality? [Testability, spec FR-001..FR-014] <!-- Evaluator: Covered by spec.md §Functional Requirements (FR-001..FR-014 each MUST-phrased with a concrete assertable behavior) -->
- [X] CHK002 Does SC-001 define an observable end-to-end signal (assistant → tool call → tool result → continue → final answer) that a fixture stream can assert as pass/fail? [Testability, spec SC-001] <!-- Evaluator: Covered by spec.md §Success Criteria SC-001 (explicit assistant→tool call→tool result→continue→final answer signal) -->
- [X] CHK003 Is "reasoning is not replayed into a subsequent provider request" (SC-002 / FR-003) stated as an inspectable property of the outgoing request, so a test can assert its absence? [Testability, spec SC-002, FR-003] <!-- Evaluator: Covered by spec.md SC-002/FR-003 + research §1 (reasoning_content not replayed into the next request; inspectable on the outgoing request) -->
- [X] CHK004 Does SC-004 specify the context-length threshold (or a way to derive it) so a fixture can place a prompt above and below it and assert the correct tier? [Testability, spec SC-004, FR-006] <!-- Evaluator: Resolved — amended SC-004 (threshold = E002 registry price-row tier boundary; fixture derives it for above/below) and plan Unit row -->
- [X] CHK005 Is "cumulative-monotonic" (SC-005 / FR-006) defined precisely enough — never decreases, never double-counts — that a test can assert it over a multi-round sequence? [Testability, spec SC-005] <!-- Evaluator: Covered by spec.md SC-005/FR-006 + Glossary + research §3 + plan HINT-003 (never decreases/double-counts; multi-round algorithm) -->
- [X] CHK006 Does SC-006 ("behaviorally indistinguishable from a Claude desk") decompose into observable peer behaviors (avatar, telemetry, memory, mailbox, autonomy/drain, breaker) each independently checkable? [Testability, spec SC-006, FR-009] <!-- Evaluator: Covered by spec.md SC-006/FR-009/US3 Independent Test (enumerates avatar, telemetry, memory, mailbox, autonomy/drain, breaker) -->
- [X] CHK007 Is "no provider-specific downstream code" (SC-006 / FR-009) expressed as a checkable condition (e.g., absence of provider branching in consumers) rather than an unverifiable claim? [Testability, spec SC-006] <!-- Evaluator: Covered by spec.md FR-009/SC-006/US3 ("no provider-specific branching in downstream consumers") -->
- [X] CHK008 Does SC-008 define what "a single clear notice" means (exactly one per capability per session) so dedupe behavior is assertable across multiple turns? [Testability, spec SC-008, FR-010] <!-- Evaluator: Covered by spec.md SC-008/FR-010/US4 AS2 + plan HINT-004 (exactly one notification per capability per session; not repeated each turn) -->
- [X] CHK009 Does SC-009 separate its three distinct assertions (no exec from partial args, transient recovery within bounded retries, runaway stops at cap) so each has its own pass/fail? [Testability, spec SC-009] <!-- Evaluator: Covered by spec.md SC-009 (three distinct clauses: no exec from incomplete args; recover within 3–5 retries without tripping breaker; runaway stops at cap/budget) -->
- [X] CHK010 Is FR-012's retryable-vs-terminal classification specified concretely enough (429/5xx/timeout retryable; 400/401/403/context-overflow terminal) that a test can assert each class routes correctly? [Testability, spec FR-012] <!-- Evaluator: Covered by spec.md FR-012 (explicit retryable allowlist 429/500/502/503/504/529/timeouts/network; terminal 400/401/403/context-overflow; exhaustive) -->
- [X] CHK011 Is the "needs credentials" guard for a missing key at launch (Edge Cases) stated as a surfaced state a test can assert, rather than only "does not start a broken loop"? [Testability, spec Edge Cases, plan Error Handling] <!-- Evaluator: Covered by spec.md Edge Cases + plan Error Handling "Missing key at launch" row (surface "needs credentials" state) -->
- [X] CHK012 For each SC, does the spec make clear which provider(s) it applies to (DeepSeek, Minimax, or both) so coverage is unambiguous? [Clarity, spec SC-001..SC-009] <!-- Evaluator: Covered by spec.md SC [US#] tags + content (SC-001/002 DeepSeek; SC-003/004 Minimax; SC-005/009 both; SC-006/007/008 both native) -->


## Fixture-Stream Coverage Definition

- [X] CHK013 Does the plan's Testing Strategy enumerate the specific DeepSeek divergences to fixture-test (index-keyed tool assembly, reasoning routing, single-final usage)? [Completeness, plan Testing Strategy] <!-- Evaluator: Covered by plan.md Testing Strategy Unit row (DeepSeek index-keyed tool assembly + reasoning routing + single-final usage) -->
- [X] CHK014 Does the plan enumerate the specific Minimax divergences to fixture-test (per-block partial-JSON assembly, thinking blocks, cumulative usage, context tier)? [Completeness, plan Testing Strategy] <!-- Evaluator: Covered by plan.md Testing Strategy Unit row (Minimax block/partial-JSON assembly + thinking + cumulative usage + context-tier scenario) -->
- [X] CHK015 Is the cumulative-monotonic usage normalization assigned to a named tier (Unit fixture) with the assertion intent stated? [Coverage, plan Testing Strategy / SC-005] <!-- Evaluator: Covered by plan.md Testing Strategy Unit row "cumulative-monotonic usage across rounds" (intent: never decreases/double-counts, SC-005/AD-004) -->
- [X] CHK016 Is the Minimax context-length-tier case (above/below threshold) called out as a fixture scenario distinct from baseline usage? [Coverage, plan Testing Strategy / SC-004] <!-- Evaluator: Resolved — added a distinct below/above-threshold fixture pair (derived from the registry tier boundary) to the plan Unit row -->
- [X] CHK017 Is the degradation no-op + single-notice (and notice-not-repeated) behavior named as a fixture/integration scenario with a clear expected outcome? [Coverage, plan Testing Strategy / SC-008] <!-- Evaluator: Resolved — plan Unit row now names "capability no-op + single-notice and notice-not-repeated across turns"; degradation.test.ts listed -->
- [X] CHK018 Is malformed/partial tool-call JSON handling defined as a fixture case asserting no tool executes and an error tool result is fed back? [Coverage, plan Testing Strategy, Error Handling / SC-009] <!-- Evaluator: Covered by plan.md Unit row "malformed/partial-JSON handling (no tool executes, error tool result fed back)" + Error Handling row -->
- [X] CHK019 Is the stream-interrupted-mid-tool-call edge case (discard incomplete block, surface retryable error) covered by the fixture-stream definition? [Completeness, spec Edge Cases, plan Error Handling] <!-- Evaluator: Resolved — added "stream-interrupted-mid-tool-call (incomplete block discarded, retryable error surfaced)" to the plan Unit fixture enumeration -->
- [X] CHK020 Is retry classification (retryable allowlist vs terminal) defined as a fixture case independent from the backoff timing behavior? [Coverage, plan Testing Strategy / FR-012] <!-- Evaluator: Covered by plan.md Unit row "retry classification (retryable allowlist vs terminal, independent of backoff timing)" + AD-006/FR-012 -->
- [X] CHK021 Does the coverage definition state which fixture cases are negative/edge (malformed JSON, interruption, refusal, runaway cap) versus happy-path multi-round loop? [Completeness, plan Testing Strategy, spec Edge Cases] <!-- Evaluator: Resolved — plan Unit row now groups cases as Happy-path / Usage-tier / Negative-edge (incl. empty/refused turn) -->
- [X] CHK022 Are the recorded SSE fixtures' provenance and shape (e.g., per-provider event sequences) defined so a fixture authentically exercises the divergence it targets? [Clarity, plan Testing Strategy, research §1–3] <!-- Evaluator: Covered by research.md §1–3 (exact per-provider SSE event sequences + usage shapes) + plan "recorded SSE fixtures" -->


## Mock Boundary Clarity

- [X] CHK023 Does HINT-001 / the plan define that each adapter's stream→turn/event core is electron-free and fetch/stream-injected so vitest runs it in Node? [Clarity, plan HINT-001, Testing Strategy] <!-- Evaluator: Covered by plan.md HINT-001 + Testing Strategy Mock Boundary ("electron-free adapter core", fetch/stream-injected, Node) -->
- [X] CHK024 Is the injection seam for `fetch`/stream stated explicitly enough that a unit test can supply a recorded stream without real network? [Testability, plan Testing Strategy "Mock Boundary"] <!-- Evaluator: Covered by plan.md Testing Strategy Unit Mock Boundary ("Inject fetch/stream ... no live network") + HINT-001 -->
- [X] CHK025 Does the plan define that only the worker entry reads `process.env`, isolating the adapter core from environment coupling for tests? [Consistency, plan HINT-001] <!-- Evaluator: Covered by plan.md HINT-001 ("the worker entry is the only place that reads process.env") + AD-002 -->
- [X] CHK026 Is the integration tier's mock boundary defined (mock adapter + mock `executeTool`, no electron) so selection + loop are tested without providers? [Clarity, plan Testing Strategy Integration row] <!-- Evaluator: Covered by plan.md Testing Strategy Integration row ("Mock adapter + mock executeTool; no electron") -->
- [X] CHK027 Does the plan define the adapter-selection test (`NATIVE_PROVIDER_ID` → factory) as a unit/integration case with an asserted selected adapter per provider id? [Coverage, plan Testing Strategy, FR-008] <!-- Evaluator: Covered by plan.md Integration row ("Adapter selection NATIVE_PROVIDER_ID → factory") + selectAdapter.test.ts -->
- [X] CHK028 Is the `runAgentLoop`-driving-a-mock-adapter multi-round scenario defined with an expected normalized event sequence to assert against? [Testability, plan Testing Strategy Integration row, FR-007/009] <!-- Evaluator: Covered by plan.md Integration row ("runAgentLoop driving a mock adapter through a multi-round tool loop emitting the normalized stream") -->
- [X] CHK029 Does the plan make clear that the additive `emit` callback (AD-001) is exercisable in tests (deltas observable) without the stub/Claude paths breaking? [Consistency, plan AD-001, HINT-005] <!-- Evaluator: Covered by plan.md AD-001 (additive emit, deltas observed via the normalized stream in the Integration row) + HINT-005 (stub/Claude paths keep compiling/behaving unchanged) -->


## No-Live-Keys-in-CI Handling

- [X] CHK030 Does the plan explicitly state that live DeepSeek/Minimax calls are out of CI and verified only by manual app-smoke? [Completeness, plan Risk Mitigation "No live API keys in CI"] <!-- Evaluator: Covered by plan.md Risk Mitigation "No live API keys in CI" row + Testing Strategy note (live calls → manual app-smoke) -->
- [X] CHK031 Does the plan assert that ALL adapter logic (assembly, usage, degradation, retry) is fixture-tested in Node, so the manual tier carries only genuine live behavior? [Consistency, plan Risk Mitigation, Testing Strategy] <!-- Evaluator: Covered by plan.md Testing Strategy note ("loop/assembly/usage/degradation logic fully fixture-tested in Node") + Risk Mitigation ("all logic" fixture/mocked) -->
- [X] CHK032 Is the boundary between fixture-tested logic and manual-smoke behavior drawn unambiguously, with no P1 logic left to the manual tier? [Consistency, plan Testing Strategy / Risk Mitigation] <!-- Evaluator: Covered by plan.md Testing Strategy note ("No P1 adapter logic is verified only here"; all logic fixture-tested, manual = live behavior only) -->
- [X] CHK033 Is the manual app-smoke scope defined (what a human confirms against real providers) rather than left implicit? [Clarity, plan Testing Strategy, Risk Mitigation] <!-- Evaluator: Resolved — added an explicit "Manual app-smoke scope" enumeration (5 confirmations tied to SC-001/002/003/005/006/007/FR-013) to the plan -->


## Key Non-Leak Test Definition (FR-013)

- [X] CHK034 Does the plan define the FR-013 key-non-leak check as a string-absence assertion over events, usage, telemetry, transcripts, and logs? [Testability, plan Testing Strategy Security row / FR-013] <!-- Evaluator: Covered by plan.md Testing Strategy Security row (string-absence check over the full FR-013 surface set) -->
- [X] CHK035 Is the surface set the non-leak test must scan enumerated (events, usage, telemetry, transcripts, hive) so the assertion is complete rather than partial? [Completeness, spec FR-013, plan Testing Strategy] <!-- Evaluator: Resolved — aligned the plan Security row surface set to FR-013's full set (events, usage, telemetry, transcripts, hive, logs) -->
- [X] CHK036 Does the test definition cover the key reaching the adapter only via the injected env seam (E004), so a leak from any other path is detectable? [Consistency, spec FR-013, plan Integration Points] <!-- Evaluator: Covered by plan.md Security row ("key reaches the adapter only through the injected env seam E004, so a leak from any other path is detectable") + Integration Points FR-008/013 -->


## Traceability & Strategy Consistency

- [X] CHK037 Does every P1 success criterion (SC-001..SC-003, SC-005..SC-007, SC-009) trace to a named test approach (fixture / integration / manual) with no P1 SC left untested? [Coverage, spec SC-001..SC-009, plan Testing Strategy] <!-- Evaluator: Covered — each P1 SC maps via its [US#]/FR to a named tier (SC-001/002/003/005→Unit fixture; SC-006/007→Integration; SC-009→Unit/Integration robustness) and the new Manual app-smoke scope explicitly names SC-001/002/003/005/006/007 for the live tier; none left untested -->
- [X] CHK038 Does each US1–US4 Independent Test line map to a defined verification tier, with the P2 US4 degradation case still fixture-covered for its logic? [Traceability, spec US1–US4 Independent Test, plan Testing Strategy] <!-- Evaluator: Covered — US1/US2 end-to-end → Integration loop + Manual live; US3 → Integration peer/selection + Manual; US4 degradation logic → Unit fixture (degradation.test.ts), confirmed fixture-covered per Testing Strategy/Risk Mitigation -->
- [X] CHK039 Are the Testing Strategy tiers and the Requirement Coverage Map mutually consistent — every FR with a fixture/integration assertion also appearing in the coverage map and vice versa? [Consistency, plan Testing Strategy / Requirement Coverage Map] <!-- Evaluator: Covered — all of FR-001..FR-014 appear in the Requirement Coverage Map and each fixture/integration assertion (assembly, usage, degradation, retry, selection, loop, key-non-leak) maps to those FRs; no orphan on either side -->
- [X] CHK040 Is each adapter robustness requirement (FR-011, FR-012) traceable to both an Error Handling Strategy row and a fixture/integration test case, with no edge case defined in the spec but absent from the test plan? [Traceability, spec FR-011/012, Edge Cases, plan Error Handling / Testing Strategy] <!-- Evaluator: Covered — FR-011→Error Handling "Malformed JSON"/"Stream interrupted" rows + Unit malformed-JSON & interruption fixtures; FR-012→"Transient"/"Non-retryable"/"Runaway" rows + Unit retry-classification; spec Edge Cases (malformed, interruption, empty/refused, runaway) now all present in the plan Unit enumeration -->

